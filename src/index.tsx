import * as React from "react";
import { render } from "react-dom";
import { App } from "./App";

// const Controls = () => <div>I am controls</div>;

// render(<Controls />, document.getElementById("controls"));
render(<App />, document.getElementById("root"));
